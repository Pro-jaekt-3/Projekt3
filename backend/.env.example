DATABASE_URL="mysql://root:password@localhost:3306/projekt3"
PORT=3000

AI_DEFAULT_PROVIDER=OLLAMA
AI_DEFAULT_MODEL=llama3.1
OLLAMA_BASE_URL=http://localhost:11434
# Timeout (ms) for real Ollama /api/generate calls (structured question drafts, etc).
# Local models can be slow; raise this further if generation still aborts. Quick
# reachability checks (models list/status) are unaffected and stay short.
OLLAMA_TIMEOUT_MS=120000

OPENAI_API_KEY=
OPENAI_BASE_URL=
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=
